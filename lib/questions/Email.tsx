/* 
 * lib/questions/Email.tsx
 */
import React from 'react';
import QuestionTemplate from './Template';

import { SharedQuestionProps } from './sharedProps';


const EmailQuestion = (props: SharedQuestionProps) => {
    return (
        <QuestionTemplate>
        
        </QuestionTemplate>        
    );
}

export default EmailQuestion;
